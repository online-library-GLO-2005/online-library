##This .env is commited because it is a school project and we are only simulating a real project. In a real project, this file should be added to .gitignore and not commited to the repository.

VITE_API_URL=http://localhost:5000